# --- project-specific (IMPORTANT) ---
# Comments must be on their own line; git does NOT strip trailing comments,
# so "config.json  # note" would be treated as a literal filename and ignore
# nothing at all.

# your league IDs / username
config.json
# API keys
secrets.json
.env
# cached Sleeper/ESPN responses
.cache/
# settings-watchdog baseline
.league_state.json
# machine-local Claude context (real league names/IDs)
CLAUDE.local.md

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environments
.venv/
venv/
ENV/
env/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.mypy_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
